apiVersion: v1
kind: Service
metadata:
  name: axion-data-simulator
  namespace: default
spec:
  selector:
    app: axion-data-simulator
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP